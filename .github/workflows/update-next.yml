name: Update next
on:
  push:
    branches:
      - main
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Rebase next to main
        run: |
            git fetch --unshallow
            git checkout next
            git rebase origin/main
            git push
